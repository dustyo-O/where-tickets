# Tasks: Document-to-Route Integration

> Each slice is end-to-end runnable. Don't move on until the slice's verification passes.

---

- [x] **Slice 1: Rename `train` → `rail` everywhere**
  - [x] Update `corpus/schema/extracted-fragment.schema.json`: change `transitTicket.documentType` enum from `train-ticket` to `rail-ticket`. **[Agent: python-backend]**
  - [x] Update `corpus/schema/expected-route.schema.json`: change `transit.mode` enum from `train` to `rail`. **[Agent: python-backend]**
  - [x] Update `corpus/generator/` so regenerated fragments and expected-routes emit `rail-ticket` / `rail`. **[Agent: python-backend]**
  - [x] Update `spikes/route_engine_llm/models.py`: `TransitMode.TRAIN = "train"` → `RAIL = "rail"`; `TransitTicketFragment.documentType` literal includes `rail-ticket` instead of `train-ticket`. **[Agent: python-backend]**
  - [x] Update `spikes/route_engine_llm/corpus.py` and `scoring.py` for the rename. **[Agent: python-backend]** _(no references found in those files; rename handled at the model layer via `TransitMode.RAIL`.)_
  - [x] Regenerate the 192-scenario corpus via `just regen-corpus`; commit the result. **[Agent: python-backend]** _(generator carries a `_SEED_MODE_ALIAS = {"rail": "train"}` internally so the per-scenario seed stays byte-stable; every emitted file uses `rail`.)_
  - [x] **Verify:** `just test-corpus` green; `just spike-engine-algo` reports 192/192; `cd backend && uv run pytest tests/spikes/` green; `just lint` clean. **[Agent: python-backend]**

- [x] **Slice 2: Drop the IATA pattern; city is a printed name + normalizer collapse**
  - [x] Remove the 3-letter pattern from `cityCode` in both schemas; rename the JSON Schema `$def` accordingly (`cityCode` → `cityName`); update all `pattern` references. **[Agent: python-backend]**
  - [x] Update the generator to emit printed city names ("Paris", "Lisbon") on legs / accommodations / expected-route stops instead of IATA codes. **[Agent: python-backend]**
  - [x] Update Pydantic models in `spikes/route_engine_llm/models.py` and `corpus.py` — drop `Field(pattern=r"^[A-Z]{3}$")` on `RouteStop.city`, `Leg.from_/to`, `ExpectedStop.city`, `ExpectedTransit.from_/to`. **[Agent: python-backend]** _(also `operations.CreateStop.city` and `HotelBookingFragment.city` — both carried the same pattern.)_
  - [x] Add a small `city_identity(name: str) -> str` helper (`strip().casefold()`) in `spikes/route_engine_llm/models.py`; route the classifier's same-city lookups through it. **[Agent: python-backend]** _(also `scoring.final_route_match` on both sides; `rules.fragment_events` dict key.)_
  - [x] Unit test: `RouteStop` with city `"Paris"` collapses with a same-fragment leg using `"PARIS"`. **[Agent: python-backend]** _(new `tests/spikes/test_city_identity.py`: 8 cases incl. whitespace variant + "stored city preserves original casing".)_
  - [x] Regenerate the 192 corpus; commit. **[Agent: python-backend]**
  - [x] **Verify:** `just test-corpus` green; `just spike-engine-algo` 192/192; `cd backend && uv run pytest tests/spikes/` green (144 passed); `just lint` clean. **[Agent: python-backend]**

- [x] **Slice 3: Replace `legs[]` with `stations[]` on transit fragments**
  - [x] Reshape `transitTicket` in `corpus/schema/extracted-fragment.schema.json`: remove `legs[]`, add `stations[]` (each `{ city, kind ∈ {airport, rail_station, bus_terminal}, identifier, departureAt?, arrivalAt? }`) and `cities[]`. **[Agent: python-backend]**
  - [x] Update the generator to emit `stations[]` (paired chronologically) and `cities[]` (city names) instead of `legs[]`. **[Agent: python-backend]**
  - [x] Add `Station` Pydantic model in `spikes/route_engine_llm/models.py`; replace `Fragment.legs` with `Fragment.stations` + `Fragment.cities`. **[Agent: python-backend]** _(also deleted the public `Leg` model; the rules derive an internal `_LegView` from `stations[]`.)_
  - [x] Add an internal `_LegView` dataclass in `spikes/route_engine_algorithmic/rules.py`; derive legs from `stations[]` by sorting by `(departureAt or arrivalAt)` and pairing departure → arrival. **[Agent: python-backend]** _(strict `dep → arr → dep → arr` alternation; bad inputs raise `RuleNotImplementedError`.)_
  - [x] Extend `RouteStop` with `stations: list[Station]` and `Transit` with `originStation` / `destinationStation`; populate them in `operations.apply` when `create_stop` / `add_transit` execute. **[Agent: python-backend]** _(also new `AddStations` op for ENRICH-path station propagation; `scoring.final_route_match` ignores the new fields so expected-route shape is unchanged.)_
  - [x] Unit tests for `_LegView` derivation across 1 / 2 / 3 / 4 station inputs (straight, return, layover, dual-direction). **[Agent: python-backend]** _(new `backend/tests/spikes/test_leg_view.py`: 9 cases incl. malformed alternation, empty input, station-object propagation, and the `final_route_match` insensitivity check.)_
  - [x] Regenerate the 192 corpus; commit. **[Agent: python-backend]**
  - [x] **Verify:** `just test-corpus` green; `just spike-engine-algo` 192/192; offline tests green; `just lint` clean. **[Agent: python-backend]**

- [x] **Slice 4: Accommodations carry `kind` + `identifier`**
  - [x] Reshape `hotelBooking` in the fragment schema: remove `city` + `checkInAt` + `checkOutAt` + `hotelName` as top-level; replace with `accommodations[]` (each `{ city, kind ∈ {hotel, airbnb}, identifier, checkInAt, checkOutAt }`) + `cities[]`. **[Agent: python-backend]** _(`kind` enum narrowed to `["hotel"]` for now; Slice 5 widens to include `"airbnb"`. Added an `accommodationDatetimes` `$def` mirroring the PDF schema convention.)_
  - [x] Update the generator to emit `accommodations[]` with `kind: "hotel"` for existing scenarios. **[Agent: python-backend]** _(also added a deterministic `accommodation_identifier` helper alongside `station_identifier` in `corpus/generator/cities.py`.)_
  - [x] Update `HotelBookingFragment` Pydantic model (rename to `AccommodationFragment`, retain `documentType: "hotel-booking"`); `Accommodation` model gains `kind: Literal["hotel", "airbnb"]` and `identifier: str`. **[Agent: python-backend]** _(also introduced a `FragmentAccommodation` model for the fragment side; `kind` literal stays `["hotel"]` in Slice 4.)_
  - [x] Update `expected-route.schema.json` `accommodation` to carry `kind` and `identifier`; regenerate expected-routes. **[Agent: python-backend]**
  - [x] Update `rules.py` event extraction to read from `accommodations[]` instead of single-shot fields. **[Agent: python-backend]** _(renamed `_build_ops_hotel` → `_build_ops_accommodation`; loops over `fragment.accommodations`, each entry independently routed through the classifier and pending-projection ledger so two same-city entries in one fragment still split correctly.)_
  - [x] Regenerate the 192 corpus; commit. **[Agent: python-backend]**
  - [x] **Verify:** `just test-corpus` green; `just spike-engine-algo` 192/192; offline tests green (158 passed); `just lint` clean. **[Agent: python-backend]**

- [x] **Slice 5: Airbnb + supplementary doc types, venues, unattached docs**
  - [x] Add `airbnb-booking` and `supplementary` to the `documentType` enum in the fragment schema and `AccommodationFragment` / new `SupplementaryFragment` Pydantic types. **[Agent: python-backend]** _(`AccommodationFragment.document_type` literal widens; new `SupplementaryFragment` is the third Fragment-union variant; schema's `hotelBooking` widens, new `supplementary` $def added to `oneOf`.)_
  - [x] Add `Venue` model + `venues: list[Venue]` to `Fragment`; extend the fragment schema to carry it. **[Agent: python-backend]** _(`FragmentVenue` lives on `SupplementaryFragment` only — transit + accommodation fragments deliberately don't carry venues. Working-route `Venue` drops the `city` since the enclosing stop names it.)_
  - [x] Add `RouteStop.venues: list[Venue]` and `WorkingRoute.unattached_documents: list[UnattachedDocument]` in `spikes/route_engine_llm/models.py`. **[Agent: python-backend]**
  - [x] Add `attach_venue` and `add_unattached_document` ops in `spikes/route_engine_llm/operations.py` (each with its applier branch). Keep `unattached_documents[]` strictly out of `final_route_match`/`identity_preserved`/`ordering_consistent`. **[Agent: python-backend]** _(`AddUnattachedDocument` is identity-clean: applier touches only `unattached_documents`, never `stops` / `transits` / id counters. `_working_stop_key` + `_expected_stop_key` already exclude `venues` / `stations`; new guard test pins the invariant.)_
  - [x] Extend `rules.build_ops` to handle airbnb (routes identically to hotel), venue-with-city (event analogous to accommodation), and supplementary-without-place (single `add_unattached_document`). Update the pending-projection ledger to include a `venues` bucket. **[Agent: python-backend]** _(airbnb rides the existing accommodation pipeline unchanged — only the `kind` literal flows through. New `EventRole.VENUE` + `_build_ops_supplementary` handler. Time anchor: `validFromAt` → `validToAt` → unanchored. Ledger gains `venue_identity` per `_Contribution` for `(kind, identifier)` dedupe. Supplementary fragments with at least one routable place are NEVER also unattached.)_
  - [x] Extend `expected-route.schema.json` with `stops[].venues` and top-level `unattachedDocuments`; update `ExpectedRoute` Pydantic models in `corpus.py`. **[Agent: python-backend]** _(both new fields optional / defaulted so the 192 existing expected-routes continue to validate.)_
  - [x] Unit tests in `backend/tests/spikes/test_rules_new_shape.py`: airbnb routed like hotel; venue-with-city attached to right stop; supplementary-without-place lands in `unattached_documents`; supplementary-with-city attaches as a venue/document on the stop without altering route shape; `add_unattached_document` does not mutate `stops[]` / `transits[]` / id counters. **[Agent: python-backend]** _(8 new tests including the multi-venue same-city + duplicate-dedupe and the `final_route_match` insensitivity guard.)_
  - [x] **Verify:** `just test-corpus` green; `just spike-engine-algo` still 192/192 (no new doc types in this corpus); `cd backend && uv run pytest tests/spikes/test_rules_new_shape.py` green; `just lint` clean. **[Agent: python-backend]**

- [x] **Slice 6: Adapter from `ExtractedFields` to `Fragment`**
  - [x] Create `backend/spikes/integration/__init__.py` and `adapter.py` with `extracted_fields_to_fragment(fields: ExtractedFields, *, source_document_id: str) -> Fragment`. Pure mapping, no I/O. **[Agent: python-backend]** _(`ExtractedFields` TypedDict defined locally — mirrors `corpus/pdf/runner.py` — to keep the production extractor's `anthropic` import out of the spike's persistent venv.)_
  - [x] Map: `document_type` (snake → kebab), datetimes (local ISO → UTC datetime), structured place lists (cities, stations, accommodations, venues), travelers, prices, qrCodes, pdfKind. **[Agent: python-backend]** _(treat-printed-as-UTC datetime convention matches the engine corpus; `pdf_kind` / `extraction_path` stay on the extractor payload — the engine fragment does not carry them. Minimum-arity guards raise `AdapterError` so violations surface at the adapter boundary rather than via opaque `ValidationError`s.)_
  - [x] Unit tests in `backend/tests/spikes/test_adapter.py` covering all six document kinds × single-leg / return / multi-traveler / missing optional datetimes / venue-with-city / venue-without-city / supplementary-with-city / supplementary-without-place. Asserts the resulting `Fragment` validates against `extracted-fragment.schema.json`. **[Agent: python-backend]** _(13 adapter assertions + 8 parametrized schema round-trips; schema validation gated per-fixture on the optional `jsonschema` package, mirroring `tests/extraction/test_schema_contract.py`. `model_dump(by_alias=True, mode="json", exclude_none=True)` matches the on-disk corpus fixture convention.)_
  - [x] **Verify:** `cd backend && uv run pytest tests/spikes/test_adapter.py` green; `just lint` clean. **[Agent: python-backend]** _(spike suite at 179 passed / 8 skipped — 166 baseline + 13 new adapter tests; `just test-corpus` clean; `just spike-engine-algo` still 192/192.)_

- [x] **Slice 7: Integration runner + one trip end-to-end (live Bedrock)**
  - [x] Create `corpus/integration/README.md` documenting the coverage matrix (placeholder for now). **[Agent: python-backend]** _(also documents the layer-1 single-purpose limitation that pushed the first integration trip to single-PDF; see below.)_
  - [x] Create `corpus/integration/01-air-return-1pax-paris-lisbon/` with `manifest.json` + `expected-route.json`. **[Agent: python-backend]** _(SINGLE-PDF trip instead of the originally-planned 3-PDF straight chain: every layer-1 PDF covers a single 2-city pair with independently-sampled travelers and dates, so no two layer-1 PDFs can be coherently chained into one trip. Multi-PDF integration trips need corpus extensions (Layer 2 or generator-driven composition) and are deferred — flagged for Slice 8. The single-PDF return ticket still exercises the adapter, the engine's `_LegView` compact-form pairing, and `final_route_match` end-to-end.)_
  - [x] Create `backend/spikes/integration/runner.py` (CLI with `--trip`, `--no-route-check`, `--json-report`) and `backend/spikes/integration/report.py` (summary + diff rendering). **[Agent: python-backend]** _(`runner.py` exposes `discover` / `run_trip` / `run_trips` / `main` and accepts an injected `extractor=` Callable so tests stub the seam without importing the production extractor. The default extractor is loaded LAZILY via dotted-path import inside `main`, mirroring `corpus/pdf/runner._load_extractor`. `report.py` provides `render_trip_summary` / `render_run_summary` / `build_report_dict` / `render_route_diff` as pure functions; the runner owns all I/O.)_
  - [x] Add `just integration *args` recipe (mirrors `just extract-pdf` isolated-venv pattern). **[Agent: python-backend]**
  - [x] `backend/tests/spikes/test_integration_runner.py`: stub-extractor tests for discovery, the unreadable-document path, the `--trip` flag, the JSON report shape. **[Agent: python-backend]** _(11 tests total: discovery (3) + happy path (1) + adapter error (1) + extraction-failure / `expect_unreadable` (2) + `--trip` filter + JSON shape (1) + `--no-route-check` (1) + `build_report_dict` shape (1) + `run_trips` loop (1). Production extractor module is never imported.)_
  - [x] **Verify:** `cd backend && uv run pytest tests/spikes/test_integration_runner.py` green (11 passed); `just integration --trip 01-air-return-1pax-paris-lisbon` runs live Bedrock and reports PASS (extraction path: vision; ~19.4s on the 8 MB rasterized PDF); `/tmp/integration-slice7.json` well-formed; `just lint` clean; engine corpus still 192/192; total spike suite 190 passed / 8 skipped (was 179 passed / 8 skipped before this slice). **[Agent: bedrock-llm]**

- [x] **Slice 8: Coverage matrix — core dimensions** _(absorbed Slice 9 in one push; see Phase-1 commit `4664ab8` + Phase-2 commit below.)_
  - [x] Add trips: `02–05` per-mode straight-line (rail, bus, mixed) + `06–09` return journeys (air, rail, bus, mixed), single traveler. **[Agent: python-backend]** _(absorbed into Slice 8 — trips 02 (single-PDF return air), 03 (rail out+hotel+back), 04 (single-PDF bus return), 05 (mixed-mode air+rail 3-city). Multi-mode rolls in via trips 17, 18, 20.)_
  - [x] Add trips: `10` two-pax-air-with-hotels; `11` airbnb-leg-2pax-rail; `12` same-city-two-airports (Paris CDG + Paris ORY collapse). **[Agent: python-backend]** _(absorbed into Slice 8 — trip 07 covers 2pax-air-with-hotels; trip 08 covers airbnb (1pax variant); trip 09 covers same-city-two-airports.)_
  - [x] Add trips: `13` divergent-travelers-mid-trip; `14` supplementary-voucher (with city); `15` supplementary-parking (without place); `16` sightseeing-venue-on-stop. **[Agent: python-backend]** _(absorbed into Slice 8 — trip 10 covers divergent-travelers; trips 11 and 13 cover supplementary-with-venue; trip 12 covers supplementary-no-location.)_
  - [x] Update `corpus/integration/README.md` coverage matrix with each trip's dimensions. **[Agent: python-backend]** _(rewritten — table now lists all 20 trips with doc kinds, traveler count, PDF count, coverage notes, plus a coverage map back to functional-spec §2.7.)_
  - [x] **Verify:** `just integration` PASS rate is 16/16; failures (if any) surface per-trip diffs; `just lint` clean. **[Agent: bedrock-llm]** _(full live integration reports 20/20 — see Phase-2 commit below.)_

- [x] **Slice 9: Scan PDFs, unreadable PDF, real-shape buffer — final 20–30 set + 100% gate** _(absorbed into Slice 8.)_
  - [x] Add trips: `17–18` scan-pdf-mixed-trip-{air,rail} (each picks at least one rasterized PDF from `corpus/pdf/layer1/`). **[Agent: python-backend]** _(absorbed into Slice 8 — trip 14 covers scan-pdf mix (rasterized air ticket + text hotel + text air); the generator's TransitPrimitive carries a `rendering` knob so any future trip can opt into rasterized renders without forking the templates.)_
  - [x] Add trip: `19` unreadable-pdf-in-trip (includes one PDF flagged `expect_unreadable: true`; expected-route is built from the rest). Tweak the runner if needed so the `expect_unreadable` branch is exercised against a live Bedrock failure. **[Agent: python-backend]** _(absorbed into Slice 8 — trip 15. Runner extended so `expect_unreadable: true` also catches the adapter-error path (zero-station extraction output), not just `ExtractionFailedError`.)_
  - [x] Add 5–10 "real-shape" trips (`20`–`30`) mirroring common European itineraries with a mix of doc types, scans, multi-traveler, and accommodations. **[Agent: python-backend]** _(absorbed into Slice 8 — trips 16–20: 5 real-shape itineraries spanning air/rail/airbnb/hotels/venues, 1pax and 2pax, with 3-city return.)_
  - [x] Finalise the README coverage matrix. **[Agent: python-backend]** _(absorbed into Slice 8.)_
  - [x] **Verify:** `just integration` reaches 20–30/20–30 PASS — the spec's 100% hard gate. JSON report includes per-trip extraction path mix and per-PDF token cost; `just lint` clean. **[Agent: bedrock-llm]** _(20/20 PASS on a single live run; see Phase-2 commit below.)_

- [x] **Slice 10: Cross-schema validation upgrade (DUS-31 done-when)**
  - [x] In `corpus/pdf/validate.py`, replace the documented mapping between `expected-fields.schema.json` and `extracted-fragment.schema.json` with a real `jsonschema.validate(sample_fragment, fragment_schema)` step over a fixture fragment derived from one layer-1 extracted-fields payload. **[Agent: python-backend]** _(new `_cross_schema_check()`: picks one payload per `document_type` (6 total), maps inline to engine `Fragment` shape (mirrors Slice-6 adapter — no cross-tree import; corpus stays independent of backend), validates against `corpus/schema/extracted-fragment.schema.json`. Stale README "Intended mapping" block replaced with active-check description.)_
  - [x] **Verify:** `just test-corpus` runs the new cross-schema validation step and is green; `just lint` clean. **[Agent: python-backend]** _("Cross-schema check passed" in test-corpus output; 2229 pytest passed (test_pdf_validator's `_clone_corpus` fixture extended to also copy `corpus/schema/extracted-fragment.schema.json`); 192/192 spike; lint clean._

- [x] **Slice 11: Retire `_SEED_MODE_ALIAS` (DUS-31 self-cleanup)**

  > Cleanup, not a feature. Deletes the known-debt shim introduced by Slice 1 (`_SEED_MODE_ALIAS = {"rail": "train"}` in `corpus/generator/scenario.py`) so future mode renames don't need it. Lands after the 100% integration gate so integration scenarios stay stable while the engine corpus reshuffles.

  - [x] In `corpus/generator/scenario.py`, replace the `_SEED_MODE_ALIAS` lookup with a stable mode key (preferred: `matrix.MODES.index(spec.primary_mode)` or a frozen `{"air": 0, "bus": 1, "rail": 2}` map). The seed payload becomes mode-rename-proof. **[Agent: python-backend]** _(used `MODES.index(spec.primary_mode)` — frozen tuple lookup, no extra constant needed.)_
  - [x] Regenerate the 192-scenario corpus; commit. **[Agent: python-backend]**
  - [x] Run `just spike-engine-algo` on the reshuffled corpus. **Path A (best case):** still 192/192 — the reshuffle didn't land on a pathological scenario; re-bless and move on. **Path B:** a small number of scenarios fail (e.g. the ZRH-revisit case flagged during Slice 1). For each failure, decide between (i) fixing the engine rule when the failure exposes a real classifier bug AND the fix is small (≤2 scenarios, narrow classifier tweak), or (ii) reverting the seed decouple and carving a separate engine ticket for the latent bug. Document the decision in the commit body. **[Agent: python-backend]** _(**Path A**: 192/192 on the reshuffled corpus first try. The Slice-1 ZRH-revisit bug either didn't get hit by this reshuffle or was fixed by structural changes in Slices 2–5 (city_identity helper, stations[] reshape, accommodation/venue work).)_
  - [x] Remove the `_SEED_MODE_ALIAS` constant and its callsite in `_seed_for`. **[Agent: python-backend]** _(constant and callsite both gone.)_
  - [x] **Verify:** `just test-corpus` green; `just spike-engine-algo` 192/192; `cd backend && uv run pytest tests/spikes/` green; `just lint` clean. **[Agent: python-backend]** _(All green. Two `test_scoring.py` cases pinned cities from the pre-reshuffle 064-circle scenario (`Rome`→`Helsinki` is the doubled city now); updated the assertions + renamed `..._double_rom` → `..._double_helsinki`.)_
