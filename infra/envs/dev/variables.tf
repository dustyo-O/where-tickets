# Per Provectus convention, root-module configuration lives in `locals.tf`
# rather than `terraform.tfvars`. This file is intentionally empty and exists
# only as a placeholder so future ad-hoc inputs (CLI overrides, CI-injected
# values) have an obvious home.
